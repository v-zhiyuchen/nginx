apiVersion: apps/v1
kind: Deployment
metadata:
  name: aspnetapp
spec:
  selector:
    matchLabels:
      app: aspnetapp
  template:
    metadata:
      labels:
        app: aspnetapp
    spec:
      containers:
        - image: czyacr.azurecr.io/webserver:latest
          name: aspnetapp
          ports:
          - containerPort: 8080